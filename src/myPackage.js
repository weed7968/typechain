// @ts-check
/**
 * Initializes the project
 * @param {object} config
 * @param {boolean} config.debug
 * @param {boolean} config.url
 * @returns
 */
export function init(config) {
  return true;
}

/**
 * Exits the profram
 * @param {number} code
 * @returns
 */
export function exit(code) {
  return code + 1;
}
